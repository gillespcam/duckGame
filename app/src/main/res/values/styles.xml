<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Water</item>
        <item name="colorPrimaryDark">@color/Shade</item>
        <item name="colorAccent">@color/White</item>
    </style>

    <style name="TitleText" parent="@style/TextAppearance.AppCompat.Medium">
        <item name="android:textSize">40sp</item>
        <item name="android:textColor">@color/White</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="ItemText" parent="@style/TextAppearance.AppCompat.Medium">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/White</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
    </style>

</resources>
